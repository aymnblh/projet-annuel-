---
description: Push current code changes to GitHub
---

1. Check current status
// turbo
2. Add all changes
3. Commit changes with a descriptive message
4. Push to remote origin

```bash
git status
git add .
git commit -m "Update: Social Feed, HQ Media, and Profile Editing"
git push origin main
```
